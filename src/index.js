import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

//Dependencies to install are:
//axio = to make get requests to the API
//react-chartjs-2 = for making charts
//react-countup = animation for changing numbers
//classnames = adding multiple classes to component
//material-ui = similar to bootsrap but more suitable for react
//chart.js = requires to run the chart-js-2 wrapper class

// From where, to where
ReactDOM.render(<App />, document.getElementById('root')); 
